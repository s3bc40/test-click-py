[project]
name = "click-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = ["click>=8.2.1"]

[project.scripts]
hello = "hello.hello:cli_mod"
digest = "digest.digest:digest"
